# Build process
Build the back end
```shell
npm run gin-build
```
Build the front end
```shell
npm run build:prod
```
Run Electron application as debug mode
```shell
npm run electron-start
```

Attention:
If you don't need to test the Electron part, you can remove the Electron module from the devDependencies in package.json.

